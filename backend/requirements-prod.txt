# Production Dependencies
# Only essential production packages
-r requirements.txt

# Production Monitoring & Logging
sentry-sdk[fastapi]==2.23.0  # Error tracking and performance monitoring
structlog==24.4.0  # Structured logging
python-json-logger==3.2.1  # JSON log formatting

# Production Performance
gunicorn==23.0.0  # WSGI HTTP Server (alternative to uvicorn in some setups)

# Health Checks
psutil==6.1.1  # System and process utilities

# Optional: Production-specific utilities
# Note: Uncomment if using these services
# boto3==1.35.88  # AWS SDK (if using AWS services)
# newrelic==10.5.0  # New Relic monitoring (if using New Relic)