# AWS Lightsail 배포용 환경 변수
# generate-secrets.sh 실행 후 실제 값으로 교체하세요

# 데이터베이스 비밀번호 (generate-secrets.sh로 생성됨)
DB_PASSWORD=change-this-secure-db-password

# JWT 시크릿 키들 (generate-secrets.sh로 생성됨)
SECRET_KEY=change-this-super-secret-key-in-production
JWT_SECRET=change-this-jwt-secret-key-in-production

# 도메인 설정 (실제 도메인 또는 IP로 변경)
DOMAIN=your-domain.com

# 환경
ENVIRONMENT=production
TZ=Asia/Seoul